<%= render "shared/header" %>

<% if user_signed_in? && @user == current_user %>
  <h1>マイページ</h1>
<% else %>
  <h1>ユーザーページ</h1>
<% end %>

<h1><%= @user.nickname %>さん</h1>

<%= render "shared/footer" %>
